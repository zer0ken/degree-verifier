package org.konkuk.common.verifier;

import org.konkuk.common.json.CriteriaData;

public class Verifier extends CriteriaData {
    protected Verifier(CriteriaData toCopy) {
        super(toCopy);
    }
}
